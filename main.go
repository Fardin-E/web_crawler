package main

import (
	"github.com/Fardin-E/web_crawler.git/backend/server"
)

// The main function is the application's entry point.
func main() {
	// Call the Run function from the server package to start the API server.
	server.Run()
}
